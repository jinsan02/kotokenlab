# P2 실행 일정 — 논문까지

> 설계는 [`SPEC_P2.md`](SPEC_P2.md), 사전 등록은 [`PLAN.md`](PLAN.md).
> 이 문서는 **순서와 시간** 만 담는다. 예측이나 판정 기준을 여기서 바꾸지 마라.

**2026-09-13 개정.** 2차를 기전 탐색 5개로 잡았던 원래 일정을 **논문 마감에서
거꾸로** 다시 짰다. 바뀐 것 셋:

1. **W0 을 신설했다** — GPU 0시간. 헤드라인에 비어 있던 논증과 계측 위생을 먼저 닫는다
2. **순서를 증거 가치로 다시 매겼다** — R1 -> Q7 게이트 -> R5 순. R2·R3 은 뒤로
3. **방어 둘을 추가했다** — downstream 평가, 초기화 baseline 명명. 원래 일정에 없었다

시간은 1차 원장 실측에서 계산했다 (168.5MB CPT 가 C0 6,237초 / T2b 4,344초).
어림한 곳은 그렇다고 적었다.

---

## 달력 — 여기서 거꾸로 짠다

```
2026-09-13   오늘
2026-11-06   EACL 2027 SRW  사전 멘토링 마감          <- 7.5주
2026-12-15   EACL 2027 SRW  직접 투고 마감            <- 13주
2027-03-09   EACL 2027 (아테네)
2027-06?     TokShop 2027   일정 미공지 — 존재 미확인
```

> **"2027 상반기 마감이라 시간 있다" 는 틀렸다.** archival 경로인 EACL SRW 는
> 올해 12월이다. 투고처 판단은 [`VENUES.md`](VENUES.md) 에 따로 둔다.

## run 하나가 얼마나 걸리는가 — 2026-09-14 실측

일정표의 시간 칸이 전부 낮게 잡혀 있었다. R1 을 돌리고 원인을 찾았다.

```
run 시간  =  기동 ~180초
           + 평가 횟수 x 25초          <- 여기를 안 셌다
           + 예산(MB) x 24~30초        <- T2b 24 / C0·손상 30
```

검산 — R1 손상 조건: `180 + 40x25 + 40x30 = 2,380초`, 원장 실측 **2,381초**.

**평가 횟수는 예산이 아니라 `--eval-bytes` 가 정한다. 기본값이 1MB 다.**
1차 본 run 들은 `--eval-bytes 20000000` 을 명시해서 8회만 돌았고, 일정표의
초/MB 는 거기서 뽑은 값이다. 오늘 나는 기본값을 써서 40회를 돌았다.
도구 버그가 아니라 **기본값이 함정** 이다.

> **앞으로 모든 CPT 명령에 `--eval-bytes` 를 명시한다.** 빼먹으면 1MB 가 되고,
> 40MB run 에서 고정비 17분이 붙는다.

평가 간격은 BPB 를 바꾸지 않는다 (평가는 비파괴적이고 `--eval-budget` 은
1MB 로 고정돼 있다). **바뀌는 것은 벽시계 시간과 곡선의 해상도뿐이다.**

그래서 촘촘한 평가는 손해만은 아니다 — 40MB run 에서 회복 곡선 점이 8개가
아니라 40개가 되고, [`plateau_nulls.md`](../reports/tables/plateau_nulls.md) 의
N1·N2 검정이 그만큼 정밀해진다. **R1~R3 은 1MB 를 유지한다.** 168.5MB 짜리는
20MB 로 간다 — 1MB 면 평가만 70분이 붙는다.

### 수정된 시간

| | 옛 추정 | 실측 기반 | `--eval-bytes` |
|---|---:|---:|---|
| Day 1 R1 (40MB x2) | 49분 | **80분** (실측) | 1MB |
| R1 판정용 T2b 40MB x1 | — | **36분** | 1MB |
| Day 2 Q7 게이트 (17.5MB x6 + S0·S1) | 1h 27m | **약 2h 10m** | 1MB |
| Day 3 R5 (168.5MB x3) | 3h 37m | **3h 45m** | 20MB |
| Day 4 Q7 S4 (168.5MB x4) | 5h 53m | **6h 05m** | 20MB |
| Day 6 R2·R3 (40MB x6) | 2h 28m | **3h 36m** | 1MB |

P2 총량 약 16시간 -> **약 19시간.**

---

```
W0   논증 · 위생        GPU 0시간         <- 지금. 마감이 없어도 여기가 먼저다
W1   R1 · Q7 게이트     2h 16m
W2   R5 · Q7 본편       9h 43m
W3   downstream · R2·R3 약 4h
W4~6 집필
```

---

## W0 — GPU 0시간 (2026-09-13~)

**학습을 한 줄도 안 돌리고 논문의 가장 큰 구멍을 메우는 구간이다.** 마감이
급해도 여기를 건너뛰면 뒤가 전부 무효가 된다.

| # | 할 것 | 왜 | 상태 |
|---|---|---|---|
| W0-1 | **sigma 를 전체 정밀도로 다시 계산한다** | `noise_floor.md` 의 sigma 가 BPB **소수 4자리 반올림** 에서 나왔다. 판정 하나가 뒤집힌다 | **완료** |
| W0-2 | 정체 자명성 논증 | "수렴이 원래 그렇지 않나" 를 반박 못 하면 헤드라인이 없다 | **완료** |
| W0-3 | R5 경쟁 등록 B | 사후 관측을 사전 예측으로 승격 | **완료** |
| W0-4 | 기울기 주장 정규화 또는 철회 | 군 크기가 3~7배 다른데 raw L2 를 비교했다 | **완료 — 철회** |
| W0-5 | 초기화 baseline 명명 확정 | FVT / FOCUS 와 정말 같은지 | **완료** |
| W0-6 | 도메인 감사 표현 정정 | "블라인드 3회" -> "감사 3회 중 블라인드 1회" | **완료** |

### W0-1 — sigma 재계산 (완료 2026-09-13)

`noise_floor.md` 의 sigma 는 BPB 를 소수 4자리로 반올림한 값에서 계산됐고,
`phase4.md` 의 T2b10k 만 전체 정밀도다. 두 표가 다른 규칙을 쓴다.

```
조건·언어      보고값      전체 정밀도
C0 ko         0.000058    0.000066
T2a ko        0.000100    0.000090
T2b30k ko     0.001234    0.001250
C0 en         0.000058    0.000031
T2a en        0.000000    0.000016     <- 0 으로 나눌 뻔했다
```

따라오는 것:

- **"21배" -> 18.9배**, **"348 sigma" -> 343.7 sigma** (T2b 판정은 유지)
- **판정 하나가 뒤집힌다** — T2a 영어. 보고는 1.9 sigma "구별 불가", 전체
  정밀도면 3.6 sigma "악화". `cpt_main.md` 가 *"T2a 의 영어만 경계다"* 라고
  이미 짚어 둔 자리다
- **en/code 의 `0.000058` 은 측정값이 아니라 반올림 양자화 바닥이다**
  (0.0001/sqrt(3) = 0.0000577). 그래서 표에 같은 값이 반복된다

한 것:

```
tools/noise_floor.py    전 조건 x 전 언어, 전체 정밀도. 표를 코드가 쓴다
tools/verdicts.py       판정도 손계산이었다 — 같은 결함이라 같이 닫았다
```

**판정에도 두 번째 잣대를 붙였다.** 판정은 seed 42 한 번의 관측을 n=3 에서
추정한 sigma 와 비교한다. 새 관측 하나에 대한 95% 예측구간은 `2 sigma` 가
아니라 `t(0.975,2) x sqrt(1+1/3) = 4.97 sigma` 로 **2.5배 넓다.**
사전 등록 규칙은 그대로 두고(RULES 14), 예측구간을 못 넘는 칸에 표시를 단다.

걸린 칸은 **T2a 영어 하나** 다 (3.6 sigma). 사전 등록 규칙으로는 "악화" 지만
예측구간을 못 넘으므로 **"이 설계로는 분해되지 않음" 으로 보고한다.**

그리고 **n=3 의 한계를 같이 적는다.** 자유도 2 면 sigma 추정의 95% CI 가
대략 `[0.52 sigma, 6.3 sigma]` 다. 0.000031 과 0.000058 을 가르는 것은 의미가
없다. 보고 규칙을 이렇게 바꾼다:

> en/code 의 sigma 는 3 seed 로 분해되지 않는다. **1e-4 미만 격차는
> "해상도 아래" 로 보고한다.** 한국어 판정(348 / 308 / 320 sigma)은 어느
> sigma 를 쓰든 흔들리지 않는다.

커밋은 `fix(eval)` + `Invalidates:` 다. 무효화되는 것은 판정 문장이지 run 이
아니므로 trailer 에 그 사실을 적는다.

### W0-2 — 정체 자명성 논증 (완료)

[`plateau_nulls.md`](../reports/tables/plateau_nulls.md) ·
`tools/plateau_nulls.py`. 학습 없음, 원장만 읽는다.

헤드라인에 대한 가장 강한 반론은 실험이 아니라 한 문장이었다 —
*"고정 예산 최적화가 남은 격차의 일정 비율을 메우는 건 수렴이 원래 그렇지 않나?"*

맞는 지적이다. 1차 완화 `dL/dD = -k(L-L*)` 를 풀면 `R = 1-e^(-kD)` 이고
**이 R 은 초기 손상과 무관하다.** 귀무모형이 이미 "조건이 달라도 R 은 같다" 를
예측한다. 반박 못 하면 헤드라인이 통째로 없어진다.

```
N1  유효 k 가 상수여야 한다        ->  422배 떨어진다            기각
N2  LR 로 정규화하면 상수여야 한다  ->  31배 남는다 (lr 은 9.2배)  기각
N3  지수모형의 등토큰 예측          ->  78.2% 예측, 실측 68.2%    9.9%p 과예측
```

**N2 가 핵심이다.** N1 이 깨지는 건 코사인 LR 이 0 으로 가니 당연할 수 있다.
LR 로 나누고도 31배가 남으므로 **감속의 대부분은 스케줄이 아니다.**

**하지 않는 주장**: 점근선. `L = L_inf + A*D^-alpha` 적합이 식별되지 않는다 —
예산 범위가 0.92 decade 뿐이라 잔차가 `L_inf` 에 단조고 내부 최소가 없다.
"무한 예산에서 몇 %에서 멈춘다" 는 이 데이터로 못 한다.

이 분석은 **사후** 다. 논문에서 exploratory 로 표시하고, 확증은 W0-3 의
등록 B 에서만 주장한다.

### W0-4 — 기울기 주장 (완료 2026-09-13 — 철회)

`phase6.md` 의 *"임베딩 기울기가 끝까지 attn 의 1.57배"* 는 군 크기를 보정하지
않은 값이다.

```
emb 136,134,656 원소   attn 44,067,840   ffn 313,786,368

                raw emb/attn    원소당 RMS emb/attn
  20MB             2.29                1.30
 160MB             1.57                0.89      <- 추세가 뒤집힌다
```

반대 방향 보정도 있다 — 임베딩 기울기는 희소하다(배치에 나온 행만 0 이 아니다).
151,936행 전체로 나누는 것도 과소평가다. **어느 쪽으로도 보정 없이는 못 쓴다.**

현재 형태의 "1.57배" 는 논문에서 뺀다. `grad_norm_emb` 의 절대값이 바닥을 안
친다(4.78 -> 2.98)는 진술만 남긴다. 제대로 하려면 셋이 필요하다 —
(a) 손상된 30,000행만의 노름 (b) grad/weight 비 (c) C0·T2a 대조군.
`phase6.md` 가 (c) 를 이미 한계로 적어 뒀다.

### W0-5 — 초기화 baseline 명명 (완료 2026-09-13)

| 우리 것 | 선행 | 판정 |
|---|---|---|
| **E1 부품 평균** | **FVT** (Gee et al., 2022) | **거의 같다. 동일하지는 않다** |
| E2 역빈도 가중 | FOCUS | **다르다** |

- **FVT** 는 새 토큰의 표면형을 원 토크나이저로 **다시 토큰화**해 나온 조각들의
  평균이다. E1 은 `id_map.json` 의 merge 부품 둘을 쓴다. T2b 는 새 토큰 =
  merge 1개라 보통 일치하지만 **구성상 보장되지 않는다** — BPE 가 그 문자열을
  다르게 쪼갤 수 있다. `init_mean.py` docstring 이 *"다시 토큰화해서 알아내야
  하는 불확실성이 없다"* 고 적어 둔 그 지점이다
  - **쟀다** (`tools/fvt_check.py`, [`fvt_check.md`](../reports/tables/fvt_check.md)):

    ```
    새 토큰              30,000
      재토큰화 정의 안 됨  4,179  (13.9%)   표면형이 유효한 UTF-8 이 아니다
      비교 가능           25,821
        일치             25,821  (100.00%)
    ```

  - **FVT 가 정의되는 곳에서는 E1 이 정확히 FVT 다.** 불일치 0건이다
  - 나머지 13.9% 는 ByteLevel merge 가 다자 문자 경계를 가로질러 토큰 하나가
    유효한 문자열이 아니다. **FVT 를 적용할 수 없는 경우** 이고, E1 이 merge
    계보를 쓴 것이 회피가 아니라 **필요** 였다는 근거가 된다
  - 표기: **"FVT 와 동등하며, FVT 가 정의되지 않는 13.9% 까지 merge 계보로
    확장한다"** + 이 표를 각주로
- **FOCUS** 는 겹치는 어휘에 대해 보조 임베딩(fasttext) 유사도로 희소 볼록결합을
  만든다. E2 는 같은 부품 둘을 **코퍼스 빈도의 역수** 로 가중한다. 유사도가
  아니라 빈도다. **"FOCUS 의 단순판" 으로 쓰면 안 된다** — "가중 평균 계열,
  가중치 출처가 다름" 까지만

이것 하나로 *"초기화 baseline 과 비교하지 않았다"* 는 지적이 사라진다.
새 실험 0건이다.

---

## W1 — 전제와 본안 게이트 (2h 16m)

### Day 1 — R1 (2026-09-14 실행, 판정 보류)

토크나이저를 안 바꾸고 임베딩 행만 망가뜨린다. **K=40**, `mean` 손상.

```
.conda/python.exe scripts/damage_rows.py --k 40 --how mean --name dmg_mean_k40
.conda/python.exe -m src.training.cpt --model artifacts/models/dmg_mean_k40 --budget-bytes 40000000 --eval-bytes 1000000 --tag r1
.conda/python.exe -m src.training.cpt --model Qwen/Qwen2.5-0.5B --revision 060db6499f32faf8b98477b0a26969ef7d8b9987 --budget-bytes 40000000 --eval-bytes 1000000 --tag r1ctrl
```

**C0 대조군은 여기서 한 번만 돌린다.** Day 2·3 과 아래 T2b 40MB 가 재사용한다.

#### 결과 (record `444f6dd`)

```
            ko 학습전   ko 최종    en 최종   code 최종
손상 K=40   2.375822  1.484565  0.839279  0.492272
C0 대조군   1.154998  1.146431  0.827927  0.484128

손상 배율   2.057배    (1차 T2b 2.058배 — K 보정이 들어맞았다)
회복률      72.50%
```

두 run 의 토큰·바이트가 완전히 같다 (11,857,920 / 40,006,921).

#### 판정을 **내리지 않았다**

원래 기준(*"40MB 기준값 59.0~59.4% 근처인가"*)이 성립하지 않는다. 그 값은 1차
T2b 곡선의 **체크포인트** 이고 R1 은 총예산 40MB 의 **종점** 이라 LR 궤적이 다르다.
정정과 새 판정 규칙은 [`PLAN.md` "R1 판정 기준 정정"](PLAN.md) 에 등록했다.

**필요한 run 하나:**

```
.conda/python.exe -m src.training.cpt --model artifacts/models/kot2b_v2_n30000_mean --budget-bytes 40000000 --eval-bytes 1000000 --tag r1base # 약 36분
```

이게 나오기 전에는 R1 을 긍정으로도 부정으로도 부르지 않는다.

> **남는 교락 — 노출.** 망가뜨린 40행은 `count_ko` 16.8M~3.3M 회이고 1차 T2b 의
> 새 토큰은 중앙값 143회다. 행당 노출이 10^4~10^5 배 다르다. K 보정은 손상
> *크기* 만 맞췄다. 판정이 무엇이든 **"임베딩 손상 일반의 성질" 이라고 부르지
> 않는다.** R2·R3 도 같은 교락을 물려받는다.

### Day 2 — Q7 게이트 (2026-09-15 실행, **구별 불가 — S4 취소**)

record `36c8b5f`. GPU 약 3h 15m.

```
S0  reserved 13,572 MB · 여유 2,703 MB     allocated(10,960)로 봤으면 오판할 뻔
S1  6/6 비트 일치, 차이 0.00e+00           파라미터 +27.6% 인데 forward 불변
S2·3
        untied T2b   untied C0   R_untied
  42      1.856245    1.149568    42.58%
 123      1.853578    1.149729    42.80%
2026      1.854490    1.149715    42.73%
 평균     1.854771    1.149671    42.70%
 sigma    0.001356    0.000089    0.113%p

R_untied 42.70%   R_tied 42.26%   ΔR +0.44%p   2 sigma 0.226%p
```

등록 규칙 `ΔR >= 5%p AND Δ > 2 sigma` — **ΔR 이 바닥의 1/11.**
**구별 불가. H 는 지지되지 않는다. S4(약 6시간) 취소.**

#### 효과 크기 바닥이 정확히 이 경우를 위해 있었다

0.44%p 가 **2 sigma 를 통과했다.** 바닥이 없었으면 "통계적으로 유의한 차이" 로
보고할 뻔했다. 2026-09-03 등록이 그 상황을 미리 적어 뒀다 —
*"ΔR 이 0.3%p 만 나와도 형식상 2 sigma 를 통과한다."*

#### 등록한 예측 둘이 모두 틀렸다

```
예측 1  회복률 72~78%            ->  42.70%
예측 2  영어는 나빠지지 않는다     ->  거의 그대로
          T2b - 자기 C0   영어         코드
          tied          +0.005656   +0.006852
          untied        +0.005730   +0.006810
```

예측 2 가 더 중요하다. 등록이 *"영어가 여전히 나빠지면 그 해석이 틀린 것이고,
그게 더 중요한 발견이다"* 라고 적어 뒀다. **부수 피해의 경로는 tie 가 아니다.**

#### 거짓 실패를 한 번 겪었다

S1 이 처음에 실패로 나왔다 — `bpb.py` 의 `--max-bytes` 기본값이 5MB 인데 1차
기준값은 2MB 로 쟀기 때문이다. **코드 BPB 가 7.6% 움직인 것이 이상해서**
모델이 아니라 평가 설정을 의심했고 config 대조로 찾았다. 그대로 믿었으면
"untie 가 깨졌다" 로 하루를 날렸다.

이 사건이 [`tools/compare_runs.py`](../tools/compare_runs.py) 를 낳았다.

---

## 남은 실험 검수 — 2026-09-15

같은 결함이 남은 실험에도 있는지 원장으로 확인했다.
**비교 대상의 `argv` 를 그대로 맞추는 것이 유일한 안전장치다.**

| 실험 | 비교 대상 | 앵커 | 전제물 | 판정 |
|---|---|---|---|---|
| **R5** | `cpt_t2b_mean_main_seed42` | 코사인 C0 (가정 등록됨) | 있음 | **플래그 셋 주의** |
| **D1·D2** | 1차 main 체크포인트 4종 | 같은 예산·스케줄 | **있음 (0.93~0.99GB x4)** | 문제 없음 |
| **D3** | 위와 같음 | 같음 | **KMMLU 데이터 없음** | **받아야 한다** |
| R2·R3 | — | — | — | **전제 상실, 재설계 대기** |

### R5 — 기본값 셋이 전부 다르다

비교 대상의 원장 argv:

```
cpt_t2b_mean_main_seed42
 --budget-bytes 168500000 --pool-docs 50000 --eval-bytes 20000000 --eval-budget 2000000
```

기본값은 `pool_docs 30000` · `eval_bytes 1000000` · `eval_budget 1000000` 이다.
**셋 다 다르다.** 그냥 돌리면 다른 문서 풀을 다른 평가 예산으로 재게 되고
비교가 무효가 된다.

```
for S in 42 123 2026; do
 .conda/python.exe -m src.training.cpt --model artifacts/models/kot2b_v2_n30000_mean --budget-bytes 168500000 --pool-docs 50000 --eval-bytes 20000000 --eval-budget 2000000 --lr-schedule constant --seed $S --tag r5
done
```

**돌리기 전에 반드시:**

```
.conda/python.exe tools/compare_runs.py cpt_t2b_mean_r5_seed42 cpt_t2b_mean_main_seed42 --allow lr_schedule
```

`lr_schedule` 만 달라야 한다. 그 외에 뭔가 걸리면 멈춘다.

> 앵커 가정과 발동 조건은 [`PLAN.md` "R1 판정 기준 정정과 R5 앵커 가정"](PLAN.md).
> R 이 66.0~73.5% 안에 떨어지면 상수 LR C0 을 1 seed 더 돌린 뒤 판정한다.

### D3 — 데이터가 없다

KMMLU 가 저장소에 없다. 받는 것 자체가 선행 작업이고, **어느 판본·어느 도메인·
몇 문항인지를 받기 전에 사전 등록해야 한다** ([`PLAN.md` "downstream 과 한자
프로브"](PLAN.md)). 데이터를 본 뒤에 고르면 사후 선택이다.

D1·D2 는 기존 체크포인트만 쓰므로 바로 돌릴 수 있다.

---

## W2 — 헤드라인 직접 검정 (9h 43m)

### Day 3 — R5 상수 LR (2026-09-16 실행, **등록 A 지지**)

record `23237cc` · `64556e2`. GPU 약 5h 25m.

```
발산 확인  17.5MB      ko 2.3803 -> 1.7458 단조 감소, lr 1e-5 유지
T2b 상수   168.5MB x3  1.473036 / 1.472577 / 1.473207
C0  상수   168.5MB x1  1.127397   (코사인 1.137540 보다 0.010 낫다)

R (상수 앵커)  72.42%   sigma_R 0.026%p   1차 코사인 65.44% 대비 +6.98%p
```

**LR 스케줄이 회복을 끊고 있었다.** 등록 A(71~75%) 지지, 등록 B(<=68.5%) 기각.

앵커 발동 조건이 실제로 켜졌고(코사인 앵커로 73.01%, 상한 73.5% 의 0.49%p 안쪽)
등록대로 상수 LR C0 을 돌렸다. C0 도 상수 LR 에서 더 나아서 R 이 0.59%p 내려갔지만
판정은 유지됐다.

**그 대조군이 수치 하나를 바로잡았다.** 처음엔 영어·코드 피해가 "1.3~1.5배" 라고
적었는데 코사인 C0 대비였다. 자기 조건 C0 대비로는 영어 +1.03% (코사인 +0.98%),
코드 +2.16% (+1.84%) 다.

#### 세 실험이 모였다

```
R1  정체는 임베딩 복구 일반의 성질이 아니다 — 치환에 특유하다   부정
Q7  tie 때문이 아니다                                           부정
R5  LR 스케줄 때문이다                                          긍정
```

**논문 헤드라인이 바뀐다.** "65% 벽" 은 코사인 스케줄 아래의 현상이고,
상수 LR 에서 72% 까지 간다. 그리고 C0 도 상수 LR 에서 나았으므로
**1차 설정 자체가 이 예산에서 덜 학습된 쪽이었을 수 있다.** 1차의 모든 절대값은
그 스케줄 조건부로 읽는다.

**열린 것:** 상수 LR 에서도 결국 멈추는가. 168.5MB 에서는 곡선이 아직 내려가고
있었다 (140->160MB 에 0.011). 이것을 물으려면 새 사전 등록이 필요하다.

### Day 4 — Q7 본편 S4 (5h 53m)

168.5MB. untied-T2b 3 seed + untied-C0 ([`SPEC_P2.md` §9.3](SPEC_P2.md) 의
발동 조건대로 1 또는 3 seed). **하루를 통째로 쓴다.**

`tools/watch_run.py` 로 감시를 건다. `fail` / `abort` 도 원장에서 지우지 않는다.

> **Q7 이 이기면 일반성은 오히려 준다.** `models.tsv` 기준 7B급은
> `tie_word_embeddings=0` 이다. tie 가 원인이면 **배포 규모에 없는 구조**의
> 이야기가 된다. 논문 서술에서 이 긴장을 숨기지 않는다
> ([`SPEC_P2.md` §9.8](SPEC_P2.md)).

---

## W3 — 방어 (약 4h)

### Day 4 (2026-09-17) — 한자 프로브 D1·D2·D3 (**완료**)

사전 등록은 [`PLAN.md` "downstream 과 한자 프로브"](PLAN.md) (2026-09-13) 와
**실행 설정 보충** (2026-09-16, 측정 전).

**등록 비교는 T2a vs C0 이다.** T2b 는 descriptive.

#### 준비 완료 (2026-09-16)

```
입력     data/interim/docs/dev_hanja.jsonl   787문서 6.71MB   등록값과 일치
         tools/build_hanja_subset.py 로 재생성 가능 (등록 수와 다르면 거부한다)
D1 설정  configs/evaluation/tokenizer_hanja.yaml   C0 / T2a / T2b
D2 입력  artifacts/models/cpt_{c0_qwen,t2a_none,t2b_mean,t2b10k_mean}_main_seed42
         전부 디스크에 있다
```

`dev_hanja.jsonl` 이 `dev` 평가를 오염시키지 않는 것도 확인했다 — 두 도구 모두
파일명이 정확히 `dev.jsonl` 이거나 `dev_control_` 로 시작해야 dev 로 읽는다.

#### 실행

```
# D1 (CPU, GPU 0)
.conda/python.exe -m src.evaluation.tokenizer_eval --config configs/evaluation/tokenizer_hanja.yaml --split dev_hanja --tag d1

# D2 (4 x 약 4분)
for M in cpt_c0_qwen_main_seed42 cpt_t2a_none_main_seed42 cpt_t2b_mean_main_seed42 cpt_t2b10k_mean_main_seed42; do
 .conda/python.exe -m src.evaluation.bpb --model artifacts/models/$M --split dev_hanja --max-bytes 7000000 --tag d2
done
```

**`--split dev_hanja` 와 `--max-bytes 7000000` 을 빼지 마라.** 빼면 각각 dev 전체와
5MB 로 돈다.

#### 판정 (등록 그대로)

```
D1   T2a tok_per_byte 가 C0 의 2% 안인가          (부분집합 전체 합계)
D2   T2a BPB 가 C0 대비 5% 안인가                 (이 부분집합의 sigma 는 없다 —
                                                   5% 미만 차이는 주장하지 않는다)
```

#### 결과 (2026-09-17) — 셋 다 예측 적중

결과는 [`reports/tables/hanja_probe.md`](../reports/tables/hanja_probe.md) 에 있다
(`tools/hanja_probe.py` 가 원장에서 쓴다). 기록 `855c07b`.

```
D1  tok_per_byte   T2a  C0 대비 +0.06%            등록 2% 안   -> 예측 적중
D2  dev_hanja BPB  T2a  C0 대비 +0.03%            등록 5% 안   -> 예측 적중
D3  KMMLU 1,900문항  C0 0.3153 [0.2947, 0.3363]   바닥 검사 통과
                    T2a-C0 -0.21%p [-1.05, +0.58]  ±2%p 안     -> 같다 (예측 적중)
```

**T2a 가 한자 포함 토큰 13,310개를 지운 것은 이 세 측정에서 흔적이 없다.**
D2 의 T2b 두 조건(+26~33%)과 D3 의 T2b(0.2368, 찍기 아래)는 descriptive 다.

읽는 법의 한계:

- D3 의 C0 는 찍기보다 약 6.5%p 위다. 바닥은 넘었지만 **0.5B 의 KMMLU 는
  감도가 낮은 계기** 다. "같다" 는 이 감도에서 같다는 뜻이다
- 한자를 품은 문항은 141개(7.4%)뿐이다. 그 문항만의 차이는 CI 가 ±3.5%p 로
  넓고, 사후 분할이라 판정에 쓰지 않는다
- CI 는 문항 표본 오차만 반영한다 (seed 42 단독)

#### D3 (KMMLU) — 등록과 실행 (2026-09-17)

등록 `6ce2e55` -> 코드 `1105df7` -> 다운로드(12파일 1.19MB, 사용자 확인 후) ->
실행 -> 기록 `855c07b`. 등록이 다운로드보다 앞선다.

아래는 2026-09-16 에 적은 준비 메모다.

데이터가 저장소에 없다. **받기 전에** 판본 · 도메인 · 문항 수 · shot · 채점 방식 ·
판정 경계를 `PLAN.md` 에 커밋한다. 데이터를 본 뒤에 고르면 사후 선택이다.
받는 것 자체도 외부 다운로드라 실행 전에 확인을 받는다.

### Day 6 — R2 · R3 (**전제를 잃었다 — 재설계 필요**)

> **2026-09-14 R1 이 부정으로 나왔다.** R2·R3 은 "임베딩 손상이 법칙" 이라는 틀
> 안에서 손상의 종류와 규모를 재는 실험이었다. 그 틀이 반증됐으므로 **지금
> 설계 그대로 돌리면 답할 질문이 없다.**
>
> 살릴 수 있는 형태가 있다. R1 이 남긴 진짜 질문은 *"손상 배율이 같은데 왜
> 21.71%p 나 벌어지나"* 이고, 후보는 셋이다 — 행당 노출 · 손상 행 수 · 토큰화
> 변경 여부. `permute` 는 분포도 노름도 보존하므로 그중 하나를 고립시킬 수 있다.
>
> **재설계 전에는 돌리지 않는다.** 사전 등록을 새로 써야 한다.

같은 K 로 `random` / `mean` / `permute` (R2), K = 10/40/160/640 (R3).
대조군은 Day 1 것을 재사용한다.

`permute` 가 무게 중심이다. 분포도 노름도 정확히 보존되고 배정만 틀리므로,
회복률이 셋 다 같으면 **"무엇이 틀렸는가" 와 무관하게 벽이 있다** 는 뜻이 된다.

> K>200 에서 영어가 무너진다 (K=300 +3.9%, K=1,000 +29%). K=640 을 보고할 때
> 영어·코드를 함께 적고 **"1차와 같은 종류의 손상" 이라고 부르지 않는다.**

### 남겨 두는 것 — 1.5B CPT · ZeTT

- **1.5B CPT** 는 조건당 0.5B 의 약 3배다. 대조군까지 최소 10시간이라 W3 에
  안 들어간다. 학습 없는 수술 + Pre-CPT BPB 만 하고, *"방향성이 유지된다"*
  이상을 주장하지 않는다
- **ZeTT** 를 못 돌리면 **왜 못 돌리는지를 limitation 에 명시한다.** 우리 결론이
  "초기화로 될 일이 아니다" 이므로, ZeTT 초기화가 같은 벽에 걸리는지가 그
  주장의 외부 검증이다 — 비워 두면 심사자가 먼저 말한다

---

## W4~6 — 집필

**2페이지 먼저, 8페이지는 확장이다.** 순서를 반대로 하면 분량에 맞추느라
헤드라인이 흐려진다.

절 순서는 **회복률 정체가 헤드라인이 되도록** 잡되, 교환비도 본론에 둔다.
정체 하나로 세우면 심사자 하나가 *"수렴이 원래 그렇다"* 로 끝냈을 때 남는 것이
없다 — W0-2 가 그 반론을 막지만, 위험은 분산해 둔다.

```
1  Introduction        빈 축: 크기를 유지한 채 바꾼다
2  Related Work        확장 / 축소 / 초기화. 교환비를 분리한 연구가 없다
3  Setup               Equal-Raw-Data · BPB · 조건별 sigma · 사전 등록
4  The trade-off       -30.2% 토큰 / +37.8% BPB / 시스템 이득        [본론 1]
5  The plateau         회복률 정의, 세 조건, 노출 4.4배 무반응        [본론 2]
6  Not a trivial fit   N1/N2/N3. 점근선은 주장하지 않는다             <- W0-2
7  What it is not      초기화 아님 · 노출 아님 · 정렬 아님 · LR 아님   <- R5
8  Is it tie?          Q7                                            <- Day 4
9  Downstream          단일 축 방어                                  <- Day 5
10 Limitations         0.5B/1.5B · dev 닳음 · 코퍼스 겹침 · ZeTT 미실행
```

2페이지 판은 **1 + 5 + 6 + 8 + 10** 만 남긴다.

---

## 매일 지키는 것

```
시작   nvidia-smi 로 여유 확인 · tools/check_clock.py --record
중간   GPU 작업은 하나만
끝     tools/validate_ledger.py · pytest tests/ -q
기록   record(...) 커밋과 코드 커밋을 섞지 않는다
```

---

## Day 0 — 준비 (2026-09-07 완료)

| 한 것 | 커밋 |
|---|---|
| 자원 프로브에 `--model` / `--out` / `--force` | `eed02d1` |
| untied 에서 `lm_head` 기울기를 잡는다 (`grad_norm_head`) | `ba4f386` |
| 상수 LR 스케줄 (`--lr-schedule constant`) | `e9e2228` |
| `scripts/untie_model.py` | `6861874` |
| `scripts/damage_rows.py` (random / mean / permute) | `bd0ad55` |
| 스모크 기록 | `9686f4f` |

만들어 둔 산출물 (`artifacts.tsv` 등록 완료):

```
artifacts/models/untied_c0_qwen     Q7 대조군
artifacts/models/untied_t2b_mean    Q7 본 조건
```

### 스모크가 확인한 것

```
untied        494,032,768 -> 630,167,424 (+27.6%)  텐서 291개  logits 비트 동일
grad 그룹      tied  emb 1 / head 0      untied  emb 1 / head 1
상수 LR        lr 1.00e-05 유지 (코사인은 같은 예산에서 1.62e-06 까지 내려감)
손상 표준편차   0.01423 -> random 0.01563 / mean 0.01057 / permute 0.01423
VRAM          tied 11,935MB  untied 12,758MB  (+823MB, 어림 +816MB 와 1% 이내)
```

### Day 1 전 보정 — 완료 (2026-09-07)

스모크가 사전 등록의 구멍 둘을 드러냈고, 학습 없이 Pre-CPT 로만 닫았다.
표는 [`k_calibration.md`](../reports/tables/k_calibration.md), 커밋 `fddcbb6`.

```
R1 의 K = 40      손상 배율 2.043배 (1차 T2b 2.058배)
R3 의 K = 10 / 40 / 160 / 640
```

**행 40개가 치환 30,000개와 같은 손상을 낸다.** 손상은 몇 행인가가 아니라
어느 행인가로 정해진다. 원래 등록했던 K=1,000 은 이미 3.114배로 과손상이었고,
1k/10k/30k/100k 라는 간격은 커버리지가 73.3% -> 83.0% 로 포화해 네 점이
사실상 한 점이었다.

---

## 알려진 부채

- **sigma 계산이 자동화돼 있지 않다** — W0-1. 이 프로젝트에서 하중이 가장 큰
  숫자인데 두 표가 다른 정밀도로 손계산돼 있다
- **산출물 등록이 수동이다.** `tools/register_artifact.py` 를 사람이 부른다.
  `run_surgery.py` 도 `untie_model.py` 도 `damage_rows.py` 도 자동 등록하지
  않는다. 1차 종료 검토가 찾은 "미등록 산출물 5개" 와 같은 뿌리다. 한 스크립트만
  고치면 경로가 갈리므로 셋을 같이 고쳐야 한다
- **원장의 `peak_vram_mb` 는 allocated 다.** 천장에 붙어 도는 run 인데 reserved
  컬럼이 없다. 스키마를 늘리는 대신 S0 의 프로브 리포트에 남긴다
  ([`SPEC_P2.md` §9.5](SPEC_P2.md))
- **등토큰 run 만 LR 축이 다르다** (`cosine_by_tokens`). 설계상 의도된 것이지만
  65.4 / 65.9 / **68.2** 를 한 표에 놓을 때 68.2 만 축이 다르다. 각주 필수
- ~~`PROMPTS.md` 의 기준값 출처 오지시와 규칙 번호 오기~~ — 2026-09-07 수정
