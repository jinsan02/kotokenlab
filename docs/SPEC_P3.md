# SPEC P3 — 스케줄 이후의 질문

> **설계 문서. 2026-09-17 작성, 아직 아무것도 돌리지 않았다.**
> 사전 등록(예측 · 판정 경계)은 [`PLAN.md` "P3 확장"](PLAN.md), 순서와 시간은
> [`SCHEDULE_P3.md`](SCHEDULE_P3.md). 규칙은 [`RULES.md`](RULES.md).
> P2 는 [`SPEC_P2.md`](SPEC_P2.md) · [`SCHEDULE_P2.md`](SCHEDULE_P2.md) 에 닫혀 있다.

## 0. P2 가 남긴 것

P2 는 "65% 회복 정체는 무엇 때문인가" 를 물었고 세 답을 얻었다.

```
R1  임베딩 복구 일반의 성질이 아니다 — 행 40개 손상은 72.50%, 치환은 50.79%   부정
Q7  tie 때문이 아니다 — untie 해도 +0.44%p                                    부정
R5  LR 스케줄 때문이다 — 상수 LR 에서 72.42%                                  긍정
```

한자 프로브(D1~D3)는 T2a 의 한자 토큰 제거가 흔적을 남기지 않았다고 답했다.

그래서 **"65% 벽" 은 헤드라인이 될 수 없다.** 코사인 스케줄의 산물이었다.
P3 는 그 자리에서 이어지는 질문 넷과 처방 하나를 묻는다.

```
포화      상수 LR 에서도 결국 멈추는가                    P3-A
불변성    "조건이 달라도 R 이 같다" 도 스케줄의 산물인가    P3-C
격차      R1 의 21.7%p 는 어디서 오는가                   P3-D · P3-E
비용      BPB 격차가 과제 점수로 얼마인가                  P3-F
처방      어휘 교체 CPT 에 어떤 스케줄을 쓸 것인가          P3-B
```

## 1. P2 에서 빠졌거나 결과로 제외된 것

| 항목 | 상태 | P3 의 처리 |
|---|---|---|
| R2 · R3 (손상 종류 · 규모) | R1 부정으로 전제 상실 | **P3-D 로 재설계.** permute 만 살린다 |
| Q7 S4 (untied 168.5MB) | 게이트에서 취소 | 종결 |
| 기울기 "attn 의 1.57배" | 군 크기 미보정으로 철회 | **P3-H 계측** — 손상 행만의 노름, update/weight 비 |
| T3 를 뺀 근거 | "전부 갈아도 회복률은 65% 로 같다" — **R1 이 반례** | 근거 문장을 정정한다 (FINAL_REPORT §10). T3 자체는 P3 범위 밖 |
| 점근선 | 0.92 decade 라 식별 불가 | P3-A 가 1.4 decade 로 넓힌다. 식별은 보장하지 않는다 |
| 헤드라인 3 seed 규약 | 코사인 T2b main 은 seed 42 단독, 상수 C0 도 1 seed | P3-B 가 개선이면 3 seed. 코사인 main 은 1차 기록으로 둔다 |
| 1.5B | 손상 K 스윕 · 추론 지표 · CPT 탐침 전부 안 함 | **P3-G 탐침.** 본 실험은 탐침 뒤 따로 등록 |
| 과제 평가 | T2b 등록 비교 없음 (D3 에서 descriptive 로만 봤다) | **P3-F**, D3 와 겹치지 않는 39과목 |
| ZeTT | 안 돌림 | **P3-E 가 "완벽한 초기화" 상한을 대신 잰다** |
| 코퍼스 편향 · Final Test | 열려 있음 | P3 밖. Final Test 는 P3 뒤에 따로 결정 |

## 2. 실험

사전 등록의 전문은 [`PLAN.md` "P3 확장"](PLAN.md) 에 있다. 여기는 왜와 어떻게만 적는다.

| # | 무엇 | 조건 | GPU (원장 실측 기반 계산) | 우선 |
|---|---|---|---:|---|
| **A** | 상수 LR 포화 | T2b · C0, 500MB | 약 8.4h | 1 |
| **B** | WSD 처방 | T2b · C0, 168.5MB (+ 개선이면 T2b 2 seed) | 2.8h (+2.4h) | 1 |
| **C** | 불변성 | T2b10k 상수 168.5MB | 약 1.2h | 1 |
| **D** | R1 격차 분해 | D0 보정 · D1 permute · D2 노출 구간(조건부) | 수 분 + 0.7h + 0.7h | 2 |
| **E** | 오라클 초기화 상한 | 이식 모델, 코사인 40MB | 약 0.6h | 2 |
| **F** | 과제 비용 | KMMLU 39과목 x 모델 4 | 약 1.8h (문항 수는 받은 뒤 확정) | 3 |
| **G** | 1.5B | VRAM 탐침 | 수 분 | 3 |
| **H** | 계측 | 코드만 | 0 | W0 |

시간 계산의 근거 (원장 `wall_sec`):

```
T2b 상수 168.5MB   4,317~4,385초      C0 상수 168.5MB   5,823초
T2b10k 코사인 168.5MB  4,051초         손상 K=40 40MB   2,381초
T2b 40MB (r1base)  1,893초            D3 KMMLU 1,900문항  약 190초/모델
```

500MB 는 168.5MB 실측에 예산 비를 곱했다 — 평가 간격(20MB)이 같으므로 평가 횟수도
같은 비율로 는다. **어림이 아니라 계산이지만 선형 가정이다.** 첫 20MB 속도로 검산한다.

F 의 문항 수는 **받기 전에는 모른다.** 39과목 test 파일의 바이트(Hub 메타데이터)를
6과목의 바이트당 문항 수로 나눈 추정이며, 받은 뒤 코드가 센다.

### A — 상수 LR 포화, R5 재현을 내장한다

R5 는 168.5MB 에서 끝났고 곡선은 아직 내려가고 있었다. 500MB 로 늘리면서
**168.5MB 까지는 R5 와 같은 run** 이 되도록 만든다. 그러면 한 run 이 재현과 확장을
동시에 한다.

지금 코드로는 그렇게 되지 않는다 — 원인이 둘이다 (§3).

### B — WSD

실무에서 가장 쓸모 있는 결과는 "어휘를 바꾼 뒤 CPT 에는 코사인을 쓰지 마라" 다.
상수 LR 은 회복률은 높지만 끝까지 LR 이 높아 감쇠에서 얻는 이득이 없다.
WSD(워밍업 · 상수 · 끝에서 감쇠)는 두 쪽을 합치려는 스케줄이다. **1차 지표를 회복률이 아니라 T2b 의 절대 BPB 로
잡는다** — WSD 는 C0 도 좋게 만들어 회복률이 오히려 내려갈 수 있고, 실무자가
원하는 것은 절대 품질이다.

### C — 불변성

P2 의 출발점은 "N · 예산 · 등토큰이 달라도 회복률이 1%p 안에서 겹친다" 였다.
R5 는 정체가 스케줄의 산물임을 보였는데, **불변성도 그런지는 모른다.** 가장 싼
조건 하나(T2b10k)로 묻는다.

### D — 격차 분해

R1 의 두 조건은 손상 배율만 같고 셋이 다르다. 기존 어휘를 망가뜨리는 방법으로는
**토큰화 변경** 을 고정한 채 나머지 둘을 옮길 수 있다. 다만 드문 행은 손상에
기여가 작아서, 노출을 맞추면 손상 배율을 못 맞출 가능성이 크다. 그래서 D0 가
게이트다 — 학습 없이 몇 분이면 안다.

permute(D1)는 R2 에서 살아남는 유일한 조건이다. 행 · 노출 · 토큰화를 R1 과 같게 두고
**손상의 종류만** 바꾼다.

### E — 오라클 초기화

ZeTT 를 못 돌리는 대신, **초기화를 이보다 잘할 수는 없는 값** 을 넣어 본다. CPT 를
마친 T2b 의 새 행이다. 이것으로도 격차가 절반 이상 남으면, 더 좋은 초기화 연구는
이 설정에서 이득이 제한적이라는 근거가 된다.

### F — 과제 비용

D3 에서 T2b 가 C0 보다 낮게 나왔지만 등록 비교가 아니었고, 그 6과목은 이미 봤다.
**본 데이터로 등록하지 않는다.** 나머지 39과목만 쓴다. 상수 LR 체크포인트는
R5 가 저장하지 않았으므로 A 의 168.5MB 저장본을 쓴다 — A 가 R5 를 재현하도록
설계한 이유가 하나 더 있는 셈이다.

### G — 1.5B

P2 SPEC §2 의 추정은 `micro_bs 1` 에서 약 15.5GB 로 경계선이었다. 탐침은 몇 분이다.
Q7 이 tie 를 원인에서 뺐으므로, 1.5B 가 들어가면 "구조가 달라서 비교할 수 없다" 는
장벽 하나가 줄어든다. **그래도 0.5B · 1.5B 두 점 밖으로 외삽하지 않는다.**

## 3. W0 — 돌리기 전에 고칠 것 (GPU 0)

P2 에서 기본값 함정만 네 번 겪었다. P3 는 **코드가 바뀌어야 하는 곳을 먼저 적는다.**

| # | 무엇 | 왜 |
|---|---|---|
| W0-1 | `cpt.py --warmup-bytes` | 워밍업이 예산의 2% 라 500MB 면 10MB 가 된다. R5 는 3.37MB 였다. **바이트로 고정해야 A 가 R5 를 재현한다** |
| W0-2 | `cpt.py --pool-extend-docs` | 문서 풀 전체를 한 번에 섞는다. 50,000 -> 늘리면 **첫 문서부터 순서가 바뀐다.** 기존 50,000개를 같은 seed 로 섞은 뒤 추가분을 뒤에 붙인다 |
| W0-3 | `cpt.py --eval-at` · `--save-at` | 20MB 간격에는 168.5MB 가 없다. 그 지점의 평가와 체크포인트가 A 의 재현 검사와 F 에 필요하다 |
| W0-4 | `callbacks.py` WSD 스케줄 | 없다 |
| W0-5 | 계측 (P3-H) | 손상 행만의 기울기 노름 · update/weight 비. `train_curve` 컬럼은 **뒤에만** 붙인다 |
| W0-6 | `damage_rows.py` · `calibrate_k.py` 순위 구간 선택 | 지금은 `count_ko` 상위 K 만 고른다 |
| W0-7 | 이식 스크립트 (E) | 없다. 이식 후 바뀌지 않은 행이 원본과 비트 단위로 같은지 검사한다 |
| W0-8 | `capability.py` 과목 집합 인자 | 지금은 D3 의 6과목이 상수다. D3 기록을 바꾸지 않게 기본값은 그대로 둔다 |
| W0-9 | R5 배증당 증분 비 도구 | A 의 예측이 (0.5, 0.85] 안인지 실행 전에 코드로 확인 |
| W0-10 | `tools/compare_runs.py` 에 새 필드 분류 | `warmup_bytes` · `pool_extend_docs` 는 치명, `eval_at` · `save_at` 은 시간 |
| W0-11 | `tools/p3_verdicts.py` | 판정을 원장에서 계산해 `reports/tables/p3_*.md` 에 쓴다. 등록 경계를 상수로 옮기고 테스트한다 |
| W0-12 | KMMLU 오염 검사 도구 | 코퍼스 파이프라인의 오염 제거 단계가 구현되지 않았다. CPT 문서 풀과 문항의 13-gram 겹침을 센다 (P3-F 판정 전, D3 6과목에도 소급해 **보고만** 한다) |

**W0-1 · W0-2 는 기본값을 바꾸지 않는다.** 인자를 안 주면 P2 와 똑같이 돌아야
과거 run 과의 비교가 유지된다. 테스트로 못 박는다.

## 4. 이 설계가 답하지 못하는 것

- A 가 "감속이 이어진다" 여도 **무한 예산의 값** 은 여전히 말하지 못할 수 있다
- D2 는 노출과 손상 행 수를 **함께** 옮긴다. 둘을 서로 가르지 못한다
- E 의 이식 행은 다른 몸통과 함께 학습됐다. "완벽한 초기화" 의 근사다
- F 는 0.5B 의 KMMLU 라 감도가 낮다 (D3 의 C0 가 찍기보다 약 6.5%p 위)
- KMMLU 가 Qwen 사전학습에 들어갔는지는 알 수 없다. W0-12 는 **우리 CPT 풀** 만 본다
- seed 는 A · C · D · E 가 1개다. R5 의 sigma_R 이 0.026%p 로 작아서 택한 절충이며,
  판정 경계 근처에 떨어지면 seed 를 늘리지 않고 **경계 근처였다고 보고한다**

## 5. 종료 조건

A~F 에 판정이 붙고 G 의 탐침 결과가 나오면 끝난다. 부정 결과도 완성이다.
그 뒤 헤드라인을 확정하고 Final Test 개봉을 따로 결정한다.
