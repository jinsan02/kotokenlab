# P3 실행 일정

> 설계는 [`SPEC_P3.md`](SPEC_P3.md), 사전 등록은 [`PLAN.md` "P3 확장"](PLAN.md).
> 이 문서는 **순서와 시간** 만 담는다. 예측이나 판정 기준을 여기서 바꾸지 마라.

작성 2026-09-17. 시간은 원장 `wall_sec` 에서 계산했다 ([`SPEC_P3.md` §2](SPEC_P3.md)).

## 달력

```
2026-09-17   P3 설계 · 사전 등록 (오늘)
2026-09-18~  W0  코드 (GPU 0) + 학습 없는 게이트 둘
             W1  싼 run 부터            약 3h
             W2  긴 run                 약 11h (밤에)
             W3  과제 비용 · 1.5B 결정   약 2h
2026-11-06   EACL 2027 SRW 사전 멘토링 마감
2026-12-15   EACL 2027 SRW 직접 투고 마감
```

GPU 합계 약 **16h** (B 의 추가 2 seed 2.4h, G 본 실험 제외).
집필은 W1 부터 병행한다 — 헤드라인은 R5 로 이미 바뀌었다.

---

## W0 — 코드와 게이트 (GPU 0 ~ 수 분)

[`SPEC_P3.md` §3](SPEC_P3.md) 의 W0-1 ~ W0-12. 순서:

1. **W0-1 · W0-2 · W0-3** (`cpt.py`) — 인자를 안 주면 P2 와 같게 도는지 테스트로 먼저 못 박는다
2. **W0-10** (`compare_runs.py`) — 새 필드를 분류하지 않으면 "미분류 = 치명" 으로 전부 막힌다
3. **W0-4 · W0-5** (WSD · 계측)
4. **W0-9** R5 배증당 증분 비 -> **P3-A 예측이 (0.5, 0.85] 안인지 확인.** 밖이면 A 를 돌리기 전에 재등록
5. **W0-6 -> D0 보정** (GPU 수 분) -> D2 를 돌릴지 결정
6. **G 탐침** (GPU 수 분) -> 1.5B 본 실험을 등록할지 결정
7. **W0-7 · W0-8** (이식 · 과목 인자)
8. **W0-11** 판정 도구 — 첫 run 이 끝나기 전에 있어야 한다
9. **W0-12** KMMLU 오염 검사 — D3 6과목에 소급 (보고만), P3-F 전에 39과목

각 코드 변경은 `feat` / `fix` 로 따로 커밋하고, run 을 시작하기 전에 전부 푸시한다.
**run 이 도는 동안 그 run 이 쓰는 모듈을 커밋하지 않는다** — 2026-09-17 D2 에서
start/ok 행의 해시가 갈렸다 (`a478426` 이 해시는 진입 시점에 고정하도록 고쳤지만,
대기열의 다음 run 은 여전히 새 코드로 돈다).

## W1 — 싼 run (약 3h)

| 순서 | run | 시간 | 비교 전 확인 |
|---|---|---:|---|
| 1 | **D1** permute K=40, 코사인 40MB | 40분 | `compare_runs.py cpt_dmg_permute_k40_* cpt_dmg_mean_k40_r1_seed42` — `model` 만 달라야 한다 |
| 2 | **E** 이식 모델 학습 전 BPB + 코사인 40MB | 35분 | `... cpt_kot2b_v2_n30000_mean_r1base_seed42` — `model` 만 |
| 3 | **D2** 노출 구간 손상 (D0 통과 시) | 40분 | D1 과 같다 |
| 4 | **C** T2b10k 상수 168.5MB | 1.2h | `... cpt_t2b_mean_r5_seed42` — `model` 만 |

40MB run 은 `--eval-bytes 1000000` (R1 과 같다), 168.5MB run 은
`--pool-docs 50000 --eval-bytes 20000000 --eval-budget 2000000` (R5 와 같다).
**빼면 기본값이 들어간다** (P2 에서 네 번 겪었다).

## W2 — 긴 run (약 11h)

| 순서 | run | 시간 |
|---|---|---:|
| 5 | **B** WSD T2b 168.5MB | 1.2h |
| 6 | **B** WSD C0 168.5MB | 1.6h |
| 7 | **A** 상수 T2b 500MB (`--warmup-bytes` · `--pool-extend-docs` · `--eval-at 168500000` · `--save-at 168500000`) | 3.6h |
| 8 | **A** 상수 C0 500MB (같은 인자) | 4.8h |

**A 는 첫 168.5MB 에서 재현 검사를 먼저 본다.** 실패하면 나머지 판정을 하지 않고
원인을 찾는다 — 재현이 안 되면 "R5 의 연장" 이라고 부를 수 없다.

B 가 "개선" 이면 T2b WSD 를 seed 123 · 2026 으로 추가한다 (2.4h).

## W3 — 과제 비용 · 1.5B (약 2h)

| 순서 | 무엇 | 시간 |
|---|---|---:|
| 9 | **F** KMMLU 39과목 다운로드 — **받기 전에 사용자 확인** (파일 수 · 크기 · 출처를 알린다) | — |
| 10 | **F** C0 코사인 · T2b 코사인 · C0 상수 · T2b 상수 | 약 1.8h |
| 11 | **G** 탐침이 통과했으면 1.5B 본 실험 등록 커밋 -> 실행 | 탐침 뒤 계산 |

## 기록

판정은 도구가 원장에서 계산해 표로 쓴다. 손으로 옮기지 않는다.

```
tools/p3_verdicts.py  (W0 에서 만든다)  -> reports/tables/p3_*.md
```

결과를 로그 파일에서 읽어 보고하지 않는다 — 2026-09-17 에 로그를 파일 이름 순서로
읽어 T2b30k 과 T2b10k 의 BPB 를 뒤바꿔 보고한 적이 있다. 표는 run_id 로 묶는다.
